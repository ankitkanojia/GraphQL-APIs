type Query  {
   greeting:String
   students: [Student],
   studentById(id:ID!):Student ,
   limitedStudents(first:Int):[Student],
   sortedStudents:[Student]
}

type Student {
   id: ID!
   firstName:String
   lastName:String
   fullName:String 
}